{
  "name": "madshare-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Madshare Android app — a Capacitor remote-URL shell that wraps the Madshare web UI (docs/architecture/android-app.md).",
  "scripts": {
    "test": "node --test tests/classify.test.mjs",
    "cap": "cap"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
